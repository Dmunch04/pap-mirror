module pap.constant;

import std.process : environment;

/// The pap cli version
public enum VERSION = "0.1.0";
/// The pap cli build type
public enum BUILD = "beta";

// ENV = environment.get("ENV_VAR", default_value);
